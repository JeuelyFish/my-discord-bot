{
  "name": "discord-bot",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d dist",
    "postinstall": "babel src -d dist",
    "run-build": "npm run build && node dist/index.js",
    "eslint": "./node_modules/.bin/eslint src; exit 0",
    "eslint-fix": "./node_modules/.bin/eslint src --fix; exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cron": "^1.3.0",
    "discord.js": "^11.3.2",
    "firebase": "^5.0.3",
    "lodash": "^4.17.10",
    "moment": "^2.22.1",
    "save": "^2.3.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "eslint": "^4.19.1",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-plugin-import": "^2.12.0"
  }
}
